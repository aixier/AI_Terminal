# 环境变量配置
# MVP阶段：所有配置已硬编码，无需.env文件
# 直接运行: docker run -d -p 3000:3000 terminal-backend

# 如需覆盖默认值，可以取消注释并设置以下变量：

# NODE_ENV=production
# PORT=3000
# LOG_LEVEL=info
# JWT_SECRET=your-secret-key
# JWT_EXPIRE_TIME=24h
# MAX_TERMINAL_SESSIONS=10
# TERMINAL_TIMEOUT=600000
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 注意：Claude功能通过已安装的claude-code CLI工具使用，
# 不需要API密钥配置

# 阿里云音视频转文字服务配置
# ALIYUN_API_KEY=your-aliyun-api-key