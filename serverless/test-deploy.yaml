edition: 3.0.0
name: test-fc
access: ai-terminal

resources:
  test:
    component: fc3
    props:
      region: cn-hangzhou
      functionName: test-ai-terminal
      runtime: nodejs20
      code: ./code
      handler: index.handler
      memorySize: 512
      timeout: 30