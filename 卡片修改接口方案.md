# 卡片修改接口方案

## 一、总体架构

### 1.1 系统流程
```
前端选择元素 → 发送修改请求 → 后端调用Claude → Claude直接修改文件 → 文件监控检测变化 → 状态更新 → 前端轮询获取状态
```

### 1.2 核心特点
- 使用 `apiTerminalService.js` 执行 Claude 命令
- Claude 直接修改文件，不返回内容
- 通过文件修改时间检测变化
- 双重监控机制确保状态准确

## 二、接口设计

### 2.1 提交修改请求

**端点**: `POST /api/html/edit`

**请求体**:
```json
{
  "htmlPath": "card/pod_demo/content_ossurl.html",  // 相对于workspace的路径
  "fileId": "content_ossurl.html",                  // 文件名
  "folderId": "pod_demo",                          // 文件夹名
  "elements": [
    {
      "tagName": "DIV",
      "className": "host-section",
      "id": "",
      "html": "<div class=\"host-section\" style=\"\">...</div>",
      "coverage": 0.013  // 1.3% = 0.013
    },
    {
      "tagName": "IMG",
      "className": "host-image",
      "id": "",
      "html": "<img src=\"...\" alt=\"主持人合照\" class=\"host-image\">",
      "coverage": 0.013
    }
  ],
  "request": "图片上移20%",  // 用户的修改需求
  "timestamp": 1705301234567
}
```

**响应**:
```json
{
  "success": true,
  "taskId": "task_1705301234567_a1b2c3",
  "status": "processing",
  "backupPath": "/app/data/backups/2024-01-15T10-20-34-567Z_content_ossurl.html",
  "message": "修改任务已创建，正在等待文件变化确认"
}
```

### 2.2 查询任务状态

**端点**: `GET /api/html/status/:taskId`

**响应（处理中）**:
```json
{
  "taskId": "task_1705301234567_a1b2c3",
  "status": "processing",
  "progress": 0,
  "filePath": "/app/data/users/default/workspace/card/pod_demo/content_ossurl.html",
  "startTime": 1705301234567,
  "mtimeBefore": 1705301234000  // 文件原始修改时间
}
```

**响应（完成）**:
```json
{
  "taskId": "task_1705301234567_a1b2c3",
  "status": "completed",
  "progress": 100,
  "filePath": "/app/data/users/default/workspace/card/pod_demo/content_ossurl.html",
  "completedAt": "2024-01-15T10:20:45.678Z",
  "duration": 11111  // 毫秒
}
```

**响应（失败）**:
```json
{
  "taskId": "task_1705301234567_a1b2c3",
  "status": "failed",
  "error": "Task timeout after 60 seconds",
  "filePath": "/app/data/users/default/workspace/card/pod_demo/content_ossurl.html"
}
```

## 三、提示词格式

### 3.1 标准格式
```
请直接编辑并修改文件: /app/data/users/default/workspace/card/pod_demo/content_ossurl.html

用户通过界面选中了以下HTML元素需要修改：

【选中元素 1】
- 标签: DIV
- 类名: host-section
- ID: (无)
- 选中覆盖率: 1.3%
- HTML内容:
<div class="host-section" style="">
    <img src="..." alt="主持人合照" class="host-image">
</div>

【选中元素 2】
- 标签: IMG
- 类名: host-image
- ID: (无)
- 选中覆盖率: 1.3%
- HTML内容:
<img src="..." alt="主持人合照" class="host-image">

用户的修改需求：
图片上移20%

要求：
1. 读取文件 /app/data/users/default/workspace/card/pod_demo/content_ossurl.html
2. 找到并修改上述选中的元素
3. 根据用户需求修改这些元素
4. 保持其他内容完全不变
5. 保持原有格式和缩进
6. 直接覆盖原文件

重要：
- 只修改用户选中的元素，不要修改其他任何内容
- 确保修改后的HTML结构正确
- 直接修改文件，不要只输出内容
```

### 3.2 提示词特点
- 包含文件绝对路径
- 包含完整的HTML元素内容
- 明确的选中覆盖率指标
- 清晰的修改需求描述
- 强调直接修改文件

## 四、文件监控机制

### 4.1 双重监控
1. **主动轮询**（主要）
   - 每500ms检查文件修改时间
   - 比较 `mtimeNow > mtimeBefore`
   - 更可靠，不受进程影响

2. **事件监听**（备用）
   - 使用 `fs.watch` 监听文件变化
   - 响应更快但可能不触发

### 4.2 状态流转
```
初始化 → processing → 检测到文件变化 → completed
                   ↓
              60秒超时 → failed
```

### 4.3 监控日志
```javascript
[FileChangeMonitor] File changed detected for task task_1705301234567_a1b2c3
[FileChangeMonitor] mtime before: 1705301234000, mtime now: 1705301245678
```

## 五、关键实现要点

### 5.1 使用 apiTerminalService
- 复用成熟的终端管理逻辑
- 避免shell转义问题
- 环境变量自动配置

### 5.2 文件备份
- 修改前自动备份
- 备份路径：`/app/data/backups/`
- 文件名包含时间戳

### 5.3 路径验证
- 确保路径在用户workspace内
- 防止路径遍历攻击
- 验证文件存在性

### 5.4 资源清理
- 监听器自动清理
- 定时器清理
- 5分钟后清理状态缓存

## 六、错误处理

### 6.1 常见错误
| 错误类型 | 处理方式 |
|---------|---------|
| 路径无效 | 返回400错误，提示路径不在workspace内 |
| 文件不存在 | 返回404错误，提示文件未找到 |
| Claude执行失败 | 返回500错误，记录详细日志 |
| 60秒超时 | 设置状态为failed，清理资源 |

### 6.2 错误恢复
- 自动从备份恢复（如需要）
- 清理终端会话
- 更新任务状态

## 七、性能优化

### 7.1 监控优化
- 500ms轮询间隔平衡性能和响应
- 60秒超时防止资源泄漏
- 双重机制提高可靠性

### 7.2 资源管理
- 及时清理监听器
- 限制缓冲区大小
- 5分钟后清理历史状态

## 八、安全考虑

### 8.1 路径安全
```javascript
// 防止路径遍历
if (filePath.includes('../') || filePath.includes('..\\')) {
  throw new Error('Invalid path: path traversal detected');
}
```

### 8.2 权限控制
- 只能修改用户自己workspace内的文件
- 文件路径必须在指定目录内
- 自动备份防止数据丢失

## 九、前端集成

### 9.1 提交流程
1. 收集选中元素
2. 构建请求数据
3. 调用 `/api/html/edit`
4. 获取 taskId

### 9.2 状态轮询
```javascript
const pollTaskStatus = async (taskId) => {
  const pollInterval = 2000; // 2秒
  const maxRetries = 30;     // 最多60秒

  const checkStatus = async () => {
    const response = await fetch(`/api/html/status/${taskId}`);
    const data = await response.json();

    if (data.status === 'completed') {
      // 成功处理
    } else if (data.status === 'failed') {
      // 失败处理
    } else {
      // 继续轮询
      setTimeout(checkStatus, pollInterval);
    }
  };

  checkStatus();
};
```

## 十、测试场景

### 10.1 功能测试
- [x] 单个元素修改
- [x] 多个元素批量修改
- [x] 复杂HTML结构修改
- [x] 样式修改（如位置调整）
- [x] 内容修改（如文本替换）

### 10.2 异常测试
- [x] 文件不存在
- [x] 路径非法
- [x] Claude超时
- [x] 并发修改

## 十一、监控指标

| 指标 | 目标值 | 说明 |
|-----|--------|------|
| 文件变化检测延迟 | < 1秒 | 从文件修改到检测到变化 |
| Claude执行时间 | < 30秒 | 正常情况下的执行时间 |
| 总完成时间 | < 35秒 | 从请求到完成的总时间 |
| 成功率 | > 95% | 成功完成的请求比例 |

## 十二、总结

本方案通过以下设计确保了卡片修改功能的可靠性：

1. **直接文件修改**：Claude直接修改文件，避免内容传递问题
2. **双重监控**：轮询+事件监听确保文件变化被检测
3. **清晰的提示词**：包含完整元素信息和明确需求
4. **完善的错误处理**：超时、备份、资源清理
5. **安全验证**：路径验证防止安全问题

整个流程清晰、可靠、安全，能够满足卡片编辑的需求。